import { strings, schedule, scheduleDays, scheduleDetails } from './strings';
import links from './links';

const Res = {
  strings, 
  schedule,
  scheduleDays,
  scheduleDetails,
  links
}

export default Res
