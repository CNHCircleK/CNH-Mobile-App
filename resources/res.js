import { strings, schedule, scheduleDays, workshopDetails } from './strings';
import links from './links';

const Res = {
  strings, 
  schedule,
  scheduleDays,
  workshopDetails,
  links
}

export default Res
